server {
    server_name {{ item.domain }};

    location / {
        try_files               $uri $uri/ @go;
    }

    location @go {
        proxy_pass              http://127.0.0.1:1323;
        proxy_set_header        Host $host;
    }
}